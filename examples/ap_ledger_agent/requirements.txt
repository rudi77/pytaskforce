# AP Ledger Agent — Dependencies
# Runs as a Taskforce plugin, no additional dependencies needed.
# SQLite is included in Python stdlib.
# Telegram integration uses the Taskforce Communication Gateway.
