[project]
name = "taskforce-butler"
version = "0.1.0"
description = "Butler agent for Taskforce - event-driven personal assistant with scheduling, rules, and Google Workspace integration"
requires-python = ">=3.11"
dependencies = [
    "taskforce",
    "google-api-python-client>=2.0.0",
    "google-auth>=2.0.0",
    "google-auth-httplib2>=0.2.0",
    "google-auth-oauthlib>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/taskforce_butler"]
